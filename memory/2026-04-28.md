## [2026-04-28 08:00 - 19:00] dev | Video Generation for ALL tiers + ElevenLabs TTS + Refactoring

### 🎬 Video Generation — Complete Rewrite & Fixes

**Decision (2026-04-28):** Video generation available for ALL subscription tiers, not just PRO.
- Free → watermark (`mojhit.pl`)
- VIP/Legend → clean video, no watermark

**Bugs fixed (8 issues resolved):**
1. `audioId` mismatch — Kie.ai API expects Suno audio ID (UUID), not Kie `task_id`. 3-level fallback: (a) `suno_id:xxx` from variant tags, (b) base64-decode from `musicfile.kie.ai/` URL, (c) KIE record-info API call, (d) task_id as fallback
2. Webhook code check — Kie callback sends `code: 0` for success, not `200`. Fixed to accept both.
3. Video status parser — Kie returns `successFlag: "SUCCESS"` (not `status`) and `response.videoUrl` (nested). Fixed.
4. Audio CORS — `musicfile.kie.ai` CDN doesn't set CORS headers. Added `/api/proxy/audio` endpoint.
5. Same video for V1/V2 — Both variants shared `kie_tasks.id`. Fixed: key video by `kie_track_variants.id` per variant.
6. Duplicate video tasks — Added dedup check before generating.
7. Missing variant 2 tracks — `audio_url` was null in callback, `stream_audio_url` had the URL. Fixed fallback.
8. Audio on track page — Tracks with `processing` status blocked video. Relaxed check.

**New features:**
- Auto-video generation: Webhook triggers video for first variant after track completion
- `/api/video/check?audio_task_id=&variant_index=` endpoint — check for existing videos
- Multiple fallbacks for Suno audio ID (tags → base64 → KIE API)
- Refreshed KIE API docs from `E:\KIE\` folder (callback format, successFlag values)

### 🎵 ElevenLabs TTS Integration

**Fixed:**
- `fetch is not a function` — `require('node-fetch')` fails on v3+ (ESM-only). Changed to `await import`.
- TTS timeout — increased from 30s to 60s (30 polls × 2s)
- Voice from admin panel not applying — `elevenlabs_voice` was stored in `theme_config` JSON but never extracted in the hook. Added `elevenlabsVoice` to producer mapping.
- Per-producer voices configured:
  - Melo MC: `Liam` (Energetyczny męski)
  - Kosa: `Roger` (Głęboki męski)
  - La Luz: `Liam`
  - CJ Remi: `River` (default)
- "Bez głosu" option added — when voice is empty, TTS handler silently ignores

**Plans documented:** `E:\KIE\mojhit-voice-plan.md` with 5 phases (custom voices → turbo TTS → dialogue → STT → sound effects)

### 🔧 Model Fix (Kosa / Grok)
- `x-ai/grok-2-vision-1212` removed from OpenRouter (404 error)
- Updated Kosa's model to `x-ai/grok-4-fast` and then to `anthropic/claude-3.7-sonnet`

### 🏗️ Architecture Refactoring (Sub-agent task)
**Generator.tsx (1200 lines) → split into:**
- `useGeneratorLogic.ts` (879 lines) — ALL logic: states, API, TTS, video, effects, handlers (Black's territory)
- `Generator.tsx` (650 lines) — pure UI skeleton calling the hook (Coder/UI territory)
- Zero conflicts, clean TypeScript build
- Commit: `39eef06`

### 🗂️ Docs & Planning
- `docs/VIDEO_GENERATION_SPEC.md` — full architecture doc (5.8KB)
- `E:\KIE\mojhit-voice-plan.md` — voice features roadmap (5 phases)
- Admin panel: Added "Aktualizuj" refresh button in Zarządzanie Wykonawcami

### 🔗 Git
- All changes pushed to `origin/main`
- Regular commits after each major fix
- Collaborators: Ant (UI/coder) and Black (logic/backend)
