## [2026-05-13 12:52] session | Полный день работы

### Серверы mojhit.pl
- Бэкенд (port 3000) и фронтенд (Vite 5173) запущены
- Supabase был на паузе (2 недели неактивности), Алекс возобновил

### 🔒 Webhook Security (Задача от Анта) — DONE
- Token-based верификация для `/api/webhooks/kie` и `/api/webhooks/kie/video`
- `KIE_WEBHOOK_SECRET` добавлен в `.env`
- Секрет передаётся через `?token=` в callback URL (kie.js, video.js)
- Без токена → 403, с токеном → 200

### 🧹 Рефакторинг server/index.js (Задача от Анта) — DONE
- Убраны дублирующиеся `require('./video')` в auto-video блоке
- Починен `/api/debug/kie-webhook-simulate`
- Убран дублирующийся `app.use(express.json())`

### 🟢 Ngrok
- Запущен: `https://fetal-hydroxide-wobbly.ngrok-free.dev` → localhost:3000
- Конфиг починен (v2→v3)

### 🧠 Agentmemory
- Установлен из GitHub (rohitg00/agentmemory)
- v0.9.12, работает через Docker (iii-engine)
- Плагин в `~/.openclaw/extensions/agentmemory/`
- `plugins.slots.memory = "agentmemory"`, `plugins.allow = ["agentmemory"]`
- Автостарт при логине: `start-agentmemory.ps1` + ярлык в Startup
- BM25+Graph search (LLM embedding отключен)

### 🎨 Open Design (nexu-io/open-design)
- Установлен в `C:\open-design`, Docker-контейнер
- v0.5.0 на порту 7456
- 80+ скиллов, 129 дизайн-систем
- **Вердикт:** сырой (1/9 успешных генераций). Остановлен, ярлык автозагрузки убран.
- Ждём v0.6+

### 🕵️ CloakBrowser
- Установлен глобально (`npm install -g cloakbrowser` + playwright-core)
- Стелс-Chromium 146, проходит Cloudflare/FingerprintJS/reCAPTCHA
- Скачан бинарник 535 MB в `~/.cloakbrowser/`
- Успешно зашёл на личный FB профиль и группу (то что API не даёт)

### 🔌 Composio
- Установлен через WSL (bash-инсталлятор)
- `composio login` → aleksize2@gmail.com / aleksize2_workspace
- Facebook подключён — 4 страницы (Zaczytajnia, MustHaben, Gripico, Donner)
- TikTok — аппа создана но не завершена (Terms/Privacy URLs нужны)
- `--install-skill openclaw` — не сработал (архив не в релизе)

### ☁️ Gemini API ключ
- Старый был слит (403), первый новый истёк (400)
- Второй новый `AIzaSy…HPDI` — работает, но rate-limited (429, free tier 5 RPM)
- Переключили embedding модель: `gemini-embedding-001` → `gemini-embedding-2` (8192 токенов)
- Memory search пока не работает из-за rate limit

### 🗑️ Qwen удалён
- Полностью удалён из конфига: provider, API key, aliases, fallbacks
- Остались: DeepSeek (основной), Gemini (fallback), OpenRouter/Minimax

### 🖥️ Инфраструктура
- Docker Desktop запущен (agentmemory, open-design)
- Windows Startup: agentmemory автозапуск
- WSL доступен для Composio и bash-скриптов

### ⚠️ Осталось на потом
- E2E тест mojhit (нужен Clerk-авторизованный запрос)
- TikTok аппа — дозаполнить когда mojhit.pl в продакшене
- Gemini rate limit — либо ждать, либо апгрейднуть план
- Memory search (ждёт сброса rate limit)
