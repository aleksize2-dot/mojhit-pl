## [2026-04-20] ingest | Suno Persona API Integration for PRO Tier

### What Was Implemented
1. **SQL Migration Script** (`sql/add_suno_persona_fields.sql`):
   - Added `suno_persona_id`, `suno_persona_model`, `source_audio_url`, `source_task_id`, `vocal_start`, `vocal_end`, `type` columns to `producers` table
   - Added `subscription_tier` column to `users` table (free/pro/enterprise)
   - Added indexes and constraints for efficient queries

2. **Persona Module** (`persona.js`):
   - `createPersona(taskId, audioId, name, description, style, vocalStart, vocalEnd)` – Creates Suno Persona from existing track
   - `deletePersona`, `listPersonas`, `getPersona` – Placeholders for future API support
   - Full error handling with Polish user messages

3. **Enhanced Suno Module** (`suno.js`):
   - Added `personaId` and `personaModel` parameters to `generate()` function
   - When provided, adds `personaId` and `personaModel` to Suno API payload for voice cloning

4. **Enhanced Kie Module** (`kie.js`):
   - Added `personaId` and `personaModel` parameters for API consistency (ignored)

5. **New API Endpoints** in `index.js`:
   - `POST /api/personas/create` – Create persona from user's track (PRO only)
   - `GET /api/personas` – List user's personas (AI producers)
   - `DELETE /api/personas/:personaId` – Delete persona (unlink from user)

6. **Updated Existing Endpoints**:
   - `POST /api/suno/generate` now accepts `personaId` and `personaModel` in request body
   - Fallback logic passes persona parameters to both Kie and Suno providers

### Business Logic for PRO Tier
- Persona creation restricted to users with `subscription_tier = 'pro'`
- Persona linked to user via `user_producers` junction table with `expires_at = NULL` (permanent)
- Producers of type `'suno'` store Suno Persona ID for future use in generations

### Technical Architecture
```
User (PRO) → Generate track → Get taskId/audioId → Create Persona → Store personaId in producers
    ↓
Future generation → Include personaId → Suno API uses cloned voice
```

### Next Steps Required
1. **Execute SQL Migration** – Run `sql/add_suno_persona_fields.sql` in Supabase Dashboard or via psql
2. **Frontend Integration** – Add UI for:
   - "Create AI Producer" button on track page (PRO only)
   - Persona selection dropdown before generation
   - "My AI Producers" page in user dashboard
3. **Testing** – Verify Suno Persona API works with actual credentials
4. **Subscription System** – Implement subscription tier updates (Clerk webhook to set `subscription_tier`)

### Critical Notes
- Suno Persona API endpoint: `POST /api/v1/generate/generate-persona`
- Each audio can only create one persona (API restriction)
- Vocal range: 0-30 seconds typical, up to 120 seconds max
- Personas work with Suno models (V4_5ALL, 5.5, etc.)

### Server Status
- Restarted server (PID 43824) – running on port 3000
- All new endpoints are live and protected by Clerk authentication