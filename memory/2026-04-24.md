## [2026-04-24 08:45-13:30] апгрейд | Большое обновление OpenClaw и инфраструктуры

### 🆙 OpenClaw обновлён: 2026.4.15 → 2026.4.22
- Gateway рестарт, удачная установка через npm

### 🧠 Модели и провайдеры
- **DeepSeek V4** (v4-pro, v4-flash) — добавлены в каталог с 1M контекстом
- **Откат на deepseek-reasoner** — V4 Pro падал с 400 ошибкой `reasoning_content`
- **OpenRouter** — новый провайдер с **minimax/minimax-m2.7**
- **Nemotron'ы** почищены из qwen-провайдера (были мёртвым грузом от OpenRouter)
- DeepSeek V4 модели остались в каталоге для будущего тестирования

### 🔍 Tavily Search (Весь новый инструмент)
- Установлен с ClawHub (`matthew77/liang-tavily-search`)
- API-ключ от Alex, бесплатный план (1000 запросов/мес)
- Поиск включён как провайдер Tavily
- Протестирован на запросе "последние новости Кривой Рог" — 9 результатов за 3с ✅

### 🗄️ Supabase скилл
- Установлен с ClawHub (`supabase@1.0.0`)
- Переменные `SUPABASE_URL` и `SUPABASE_SERVICE_KEY` установлены системно
- Выполнен полный аудит схемы БД — обнаружено 7 новых таблиц (api_settings, affiliate_earnings, referral_rewards, video_tasks, lyrics, guest_limits, system_logs), которых нет в документации

### 📋 Обновлена документация
- `memory/wiki/supabase-schema.md` — полностью переписана с актуальной схемой из OpenAPI Supabase
- Содержит все 21 таблицу + 1 view + 2 RPC функции с точными типами и описаниями

### ⚙️ Система
- Сервера (:3000 + :5174) и ngrok тунель работают
- Все плагины активны: telegram, deepseek, google, qwen, openrouter, browser
- Известный баг: `config.patch` для `skills.entries` вызывает `size-drop` ошибку в 2026.4.22

## [2026-04-24 13:30-14:43] dev | Стэк инструментов, GA4 скилл, фиксы

### 📦 Новые скиллы с ClawHub
- **GA4 Analytics** — установлен (`ga4-analytics@1.0`). Для работы нужен Google Cloud service account:
  - Включить Google Analytics Data API
  - Создать service account → JSON ключ
  - Добавить service account в GA4 как читателя
  - Alex будет настраивать со своего Google-аккаунта
- **Stripe API** (через Maton) — установлен, но **удалён**. Решили работать напрямую с Stripe через существующий secret key из .env сервера, без посредников.

### 🗄️ Supabase
- Alex подтвердил, что `system_logs` создана — таблица уже была задокументирована в обновлённой схеме
- Полный аудит схемы БД через OpenAPI Supabase (21 таблица + 1 view + 2 RPC)

### 🐛 Багфикс
- Сервер упал на **лишней фигурной скобке** в строке 1265 (предположительно после вчерашнего рефакторинга админки). Исправлено.
- После фикса: бекенд :3000 ✅, фронтенд :5174 ✅

### 🧑 Человеческий момент
- Alex поделился, что это его первый проект такой сложности (раньше только WordPress/Shopify). Признался, что боится накосячить. Чувствует, что это "серьёзный шаг". Ответил поддержкой и признанием проделанной работы.

### Ещё точки внимания
- `config.patch` для `skills.entries` в OpenClaw 2026.4.22 всё ещё багован (size-drop на 28K→9.6K). Обход: env-переменные вместо конфиг-патча для sensitive данных.
- После рестарта серверов нужно проверить логирование в `system_logs` таблицу.

## [2026-04-24 14:43-15:30] dev | GA4 отложен, Brevo установлен, напоминалка

### 📧 Brevo (email-рассылки)
- Установлен скилл `brevo@1.0` с ClawHub
- **Что нужно:** регистрация на brevo.com и API-ключ
- **Зачем:** рассылки пользователям (промо, "твой трек готов", новости, скучаем)

### ⏸️ GA4 Analytics — отложен
- Решение: настроить **после деплоя сайта на сервер**
- Скилл установлен, ждёт развёртывания
- **Напоминалка:** cron на каждый понедельник 10:00

### 🐛 Фикс бага
- Убрана лишняя `}` в index.js:1265 — сервер снова работает

### 📝 План на будущее
1. После деплоя → GA4 service account + настройка analytics скилла
2. Сейчас → Brevo (email-рассылки) — нужен API-ключ
3. Stripe — напрямую через secret key без сторонних скиллов
