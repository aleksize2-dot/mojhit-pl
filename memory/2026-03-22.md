# 2026-03-22

## System & Tools
- **Lenovo T430 (Agent Kimi):** 
  - Investigated API options and model selection (NVIDIA NIM vs. Google Gemini vs. DeepSeek).
  - Configured `openclaw.json` to switch the primary model to **DeepSeek V3.2** (`deepseek-chat`) via the official API (`https://api.deepseek.com`).
  - Resolved `Unrecognized key: "tools"` configuration errors specific to OpenClaw `2026.3.13` by stripping deprecated channel tool blocks.
  - Successfully killed stale/conflicting gateway processes (`architect.service`) and stabilized the `openclaw-gateway.service`.
  - Addressed user concerns regarding the automatically provisioned `claw_boss` sandboxing account on Windows.
  - Diagnosed agent unresponsiveness, providing commands for session management and log trailing (`openclaw logs --follow`).

## Bubble.io Project: mojhit.pl
- **Status:** Frontend HTML/CSS/JS prototype (`mojhit_allegro_final.html`) is complete. Ready for integration into Bubble.io (either via raw HTML embedding or manual block recreation).
