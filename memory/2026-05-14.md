## [2026-05-14] session | Полный день работы над mojhit.pl

### 👥 Совместная работа с Антом
- Ант работал через Control UI (сессия openclaw-control-ui)
- Я (Black) — основная сессия, бекенд + координация
- Найдены и пофикшены критичные баги видео и БД

### 🎬 Видео — главная победа дня
- **Корень:** `tracks.video_url` колонка не существовала — миграция не была применена
- Ант применил миграцию, добавил video polling (15s), backfill для существующих треков
- Видео callback URL были без токена → 403 (Ант пофиксил все 5 мест)
- `video_tasks_audio_task_id_fkey` constraint мешал variant-specific ID (Ант дропнул)
- `audio_task_id` в polling не выбирал `user_id` → NOT NULL violation (Ант пофиксил)
- `audioId` в polling авто-видео передавался как mp3 URL вместо suno_id (я пофиксил)
- V1/V2 показывали одинаковое видео — хардкод `variant_index=0` в API (Ант пофиксил API и фронтенд)

### 🎤 CJ Remi — прокачка до v3.0 (Modern Disco / Pop-Folk)
- Убраны "Jefie" и ночная тема по умолчанию
- init_msg с примерами оказий (🎂💒🚗🌴😊❤️🔥)
- BPM 115-120 → 122-128, добавлен Slap House + brass + biesiada
- Post-Chorus в структуре LYRIC
- Правила: не давить после генерации, без имён → сразу генерить, оказия ≠ только день рождения
- Промпт сохранён в Supabase и синхронизирован в CJ_REMI_PRODUCER.md (v3.0)

### 🖥️ Бекенд — инфраструктура
- **Провайдеры:** kie.ai (активный), sunoapi.org (фолбэк) — поменяны местами
- KIE apiKey синхронизирован в api_settings (был пустой)
- **Suno webhook:** создан POST /api/webhooks/suno с токеном
- **Suno callback URL:** localhost → ngrok (fetal-hydroxide-wobbly.ngrok-free.dev)
- **Авто-поллинг:** 10 секунд (было 30), проверяет KIE + Suno, + video_polling (15s)
- **Статус-эндпоинт:** read-only (пишут только webhook + polling)
- **UPSERT → DELETE+INSERT** с try/catch на duplicate (23505) для kie_track_variants и tracks
- Все 5 точек записи в kie_track_variants защищены от race condition
- `suno_version` теперь реально используется при генерации (раньше игнорился)

### 🎭 Исполнители
- **Kosa:** модель DeepSeek V4 Flash, `suno_version: V5`, создан KOSA_PRODUCER.md и la_luz.js
- **CJ Remi, Melo MC, La Luz:** Claude Sonnet 4.6
- **Админка:** модельки обновлены (+ Grok 4.3, 4.20, 4 Fast, DeepSeek V4 Flash), Xiaomi убран
- `server/producers/*.js` синхронизированы с Supabase
- OpenRouter apiKey синхронизирован в api_settings

### 📧 Email
- `emailService.js` готов, не хватает SMTP-кредами (закомментированы в .env)

### ⚠️ Осталось
- SMTP для email-уведомлений
- Оба провайдера хранят файлы 14 дней — добавить напоминание в email
- Полный E2E тест с видео на свежей генерации

### 🗂️ Файлы
- `server/index.js` — много правок (webhook, polling, generate)
- `server/video.js` — универсальный для KIE + Suno
- `server/suno.js` — callback URL с токеном, 404 не спамит в error
- `server/kie.js` — 404 не спамит в error
- `server/.env` — SUNO_CALLBACK_BASE_URL, SUNO_WEBHOOK_SECRET
- `server/producers/` — cj_remi.js, kosa.js, melo_mc.js, la_luz.js синхронизированы
- `client/useGeneratorLogic.ts` — защита от битого JSON, сообщение про Moje Utwory
- `client/TrackDetail.tsx` — variant_index fix, защита .json()
- `client/admin/ProducerManager.tsx` — модели обновлены, Xiaomi убран
- `docs/CJ_REMI_PRODUCER.md` — v3.0
- `docs/KOSA_PRODUCER.md` — создан
