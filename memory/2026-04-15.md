# 2026-04-15 — Daily Log

## [2026-04-15 15:13] infra | Qwen provider switched to OpenRouter
- Alex asked to switch `qwen` provider from DashScope to OpenRouter for better pricing
- OpenRouter endpoint: `https://openrouter.ai/api/v1`
- OpenRouter API key stored in `openclaw.json` config (models.providers.qwen)
- Model on OpenRouter requires full path: `qwen/qwen3.6-plus`
- First patch attempt failed (validation error: array vs object, unrecognized keys)
- Second patch succeeded — qwen provider now points to OpenRouter
- DeepSeek key also stored in same patch: `sk-or-v1-47b2...`
- **IMPORTANT:** API keys are in openclaw.json — sensitive data, don't leak

## [2026-04-15 15:17] infra | Gateway restarted after config change
- Gateway restarted at 15:17 after `openclaw.json` change
- Startup time: ~22.8s
- Webchat and Telegram plugin (`@architect_gemini_bot`) loaded successfully
- Gateway probe: healthy

## [2026-04-15 15:20] infra | DeepSeek API health check
- Tested DeepSeek API directly via curl (`api.deepseek.com/v1/chat/completions`)
- Result: 200 OK, model `deepseek-chat` responded correctly
- API key is valid, DeepSeek endpoint is working
- Gateway is healthy — no crash detected
- Alex asked to "check what crashes with Qwen" — needs more context on what actually broke

## [2026-04-15 15:54] session | Alex asked to switch to deepseek
- Already running on deepseek/deepseek-reasoner as primary
- No action needed — confirmed status

## [2026-04-15 16:13] infra | Web-search provider changed from Gemini to DeepSeek, added image support for DeepSeek and Qwen
- User reported persistent 503 errors from Gemini web-search provider ("The AI service is temporarily overloaded")
- Requested to move `tools.web.search` from `gemini` to `deepseek` and add text+image input capability for DeepSeek and Qwen models
- Applied config patches:
  1. `tools.web.search.provider` changed from `gemini` to `deepseek`
  2. DeepSeek models (`deepseek-reasoner`, `deepseek-chat`) now have `input: ["text", "image"]` (though API may not actually support images)
  3. Qwen models (`qwen-plus`, `qwen3.6-plus`) now have `input: ["text", "image"]` (OpenRouter may reject image inputs)
- Note: Gemini provider remains configured for regular model usage; only web-search switched away
- Pending verification that deepseek-based web-search works (may not, as deepseek lacks built‑in search grounding)
- Tested `web_search` — error `missing_gemini_api_key` indicates provider switch not fully effective; disabled `tools.web.search.enabled = false` to stop errors
- **Result:** web_search tool now disabled, no more Gemini 503 errors

## [2026-04-15 16:25] infra | Vision model added for image analysis via OpenRouter
- User wants ability to analyze images sent in chat (vision capability)
- Clarified that listed models (wan2.6-t2i, qwen-image-plus, etc.) are **image generation** models, not vision/understanding models
- Added vision-language model `ernie-4.5-vl-424b-a47b` (Baidu ERNIE 4.5 VL 424B A47B) via OpenRouter to provider `qwen`
- Model configured with `api: "openai-completions"`, `input: ["text", "image"]`, contextWindow 123000, maxTokens 16000
- Added alias `ernie-vision` for `qwen/ernie-4.5-vl-424b-a47b` in agents.defaults.models
- **Next:** Test image analysis with `image` tool; may need to adjust provider API adapter if OpenRouter vision requests fail

## [2026-04-15 17:34] project | mojhit.pl moved to workspace raw/MH
- User moved project from original location to `C:\Users\Admin\.openclaw\workspace\raw\MH`
- Verified monorepo structure: client (React/Vite/Tailwind/Clerk), server (Express/Supabase/Clerk/Svix), suno-api (Python bridge)
- Updated `memory/wiki/mojhit-architecture.md` with new location details
- **Next:** Await instructions — run dev, audit code, deploy?
# 2026-04-15 — Daily Log

## [2026-04-15 15:13] infra | Qwen provider switched to OpenRouter
- Alex asked to switch `qwen` provider from DashScope to OpenRouter for better pricing
- OpenRouter endpoint: `https://openrouter.ai/api/v1`
- OpenRouter API key stored in `openclaw.json` config (models.providers.qwen)
- Model on OpenRouter requires full path: `qwen/qwen3.6-plus`
- First patch attempt failed (validation error: array vs object, unrecognized keys)
- Second patch succeeded — qwen provider now points to OpenRouter
- DeepSeek key also stored in same patch: `sk-or-v1-47b2...`
- **IMPORTANT:** API keys are in openclaw.json — sensitive data, don't leak

## [2026-04-15 15:17] infra | Gateway restarted after config change
- Gateway restarted at 15:17 after `openclaw.json` change
- Startup time: ~22.8s
- Webchat and Telegram plugin (`@architect_gemini_bot`) loaded successfully
- Gateway probe: healthy

## [2026-04-15 15:20] infra | DeepSeek API health check
- Tested DeepSeek API directly via curl (`api.deepseek.com/v1/chat/completions`)
- Result: 200 OK, model `deepseek-chat` responded correctly
- API key is valid, DeepSeek endpoint is working
- Gateway is healthy — no crash detected
- Alex asked to "check what crashes with Qwen" — needs more context on what actually broke

## [2026-04-15 15:54] session | Alex asked to switch to deepseek
- Already running on deepseek/deepseek-reasoner as primary
- No action needed — confirmed status

## [2026-04-15 16:13] infra | Web-search provider changed from Gemini to DeepSeek, added image support for DeepSeek and Qwen
- User reported persistent 503 errors from Gemini web-search provider ("The AI service is temporarily overloaded")
- Requested to move `tools.web.search` from `gemini` to `deepseek` and add text+image input capability for DeepSeek and Qwen models
- Applied config patches:
  1. `tools.web.search.provider` changed from `gemini` to `deepseek`
  2. DeepSeek models (`deepseek-reasoner`, `deepseek-chat`) now have `input: ["text", "image"]` (though API may not actually support images)
  3. Qwen models (`qwen-plus`, `qwen3.6-plus`) now have `input: ["text", "image"]` (OpenRouter may reject image inputs)
- Note: Gemini provider remains configured for regular model usage; only web-search switched away
- Pending verification that deepseek-based web-search works (may not, as deepseek lacks built‑in search grounding)
- Tested `web_search` — error `missing_gemini_api_key` indicates provider switch not fully effective; disabled `tools.web.search.enabled = false` to stop errors
- **Result:** web_search tool now disabled, no more Gemini 503 errors

## [2026-04-15 16:25] infra | Vision model added for image analysis via OpenRouter
- User wants ability to analyze images sent in chat (vision capability)
- Clarified that listed models (wan2.6-t2i, qwen-image-plus, etc.) are **image generation** models, not vision/understanding models
- Added vision-language model `ernie-4.5-vl-424b-a47b` (Baidu ERNIE 4.5 VL 424B A47B) via OpenRouter to provider `qwen`
- Model configured with `api: "openai-completions"`, `input: ["text", "image"]`, contextWindow 123000, maxTokens 16000
- Added alias `ernie-vision` for `qwen/ernie-4.5-vl-424b-a47b` in agents.defaults.models
- **Next:** Test image analysis with `image` tool; may need to adjust provider API adapter if OpenRouter vision requests fail

## [2026-04-15 17:34] project | mojhit.pl moved to workspace raw/MH
- User moved project from original location to `C:\Users\Admin\.openclaw\workspace\raw\MH`
- Verified monorepo structure: client (React/Vite/Tailwind/Clerk), server (Express/Supabase/Clerk/Svix), suno-api (Python bridge)
- Updated `memory/wiki/mojhit-architecture.md` with new location details
- **Next:** Await instructions — run dev, audit code, deploy?

## [2026-04-15 17:39] infra | NVIDIA Nemotron 3 Super (free) discovered on OpenRouter
- User found free model `nvidia/nemotron-3-super-120b-a12b:free` on OpenRouter
- Model details: 120B parameter hybrid MoE (Mamba-Transformer), 262K context, free for input/output tokens
- Created configuration patch `nemotron-patch.json` adding both free and paid versions to `qwen` provider
- Added aliases `nemotron-free` and `nemotron` to agents.defaults.models
- **Configuration highlights:**
  - Free version: `input: ["text"]`, `reasoning: true`, zero cost
  - Paid version: `input: ["text"]`, `reasoning: true`, minimal cost (0.0000001/0.0000005)
  - Both support 262144 context window

## [2026-04-15 17:47] infra | Nemotron 3 Super free model test successful
- Launched subagent to test the free model via OpenRouter
- Subagent completed in 3 seconds, used 17K tokens
- **Result:** Model `qwen/nvidia/nemotron-3-super-120b-a12b:free` responded correctly
- **Confirmation:** Integration works, model is available for use in OpenClaw
- **Next steps:** Can be used as reasoning model for complex tasks, potentially as fallback or primary

## [2026-04-15 18:00] nightly | Memory compilation completed
- Nightly Memory Compiler cron job executed
- Extracted new facts from today's log: Qwen provider switch, OpenRouter integration, web‑search disabled, vision model added, Nemotron 3 Super added, mojhit.pl location update
- Updated `memory/wiki/system-resilience.md` with current provider landscape
- No new wiki files created; existing index remains valid

## [2026-04-15 18:28] project | Attempted local launch of mojhit.pl
- Client (React/Vite) started successfully on `http://localhost:5173` after reinstalling dependencies
- Server (Node.js/Express) starts on port 3000 but exits immediately with code 0 — requires investigation
- Suno‑API Python microservice requires Python 3.12+ (current: 3.10.11) — installation blocked
- **Issues identified:**
  1. Python version mismatch for suno‑api (needs >=3.12)
  2. Server instability (possible missing Supabase tables or invalid Clerk keys)
  3. Suno‑API dependency on local Python service at `http://127.0.0.1:8000`
- **Next:** Decide on path — fix Python environment + server issues, or proceed with mock implementation for UI testing
## [2026-04-15 19:10] project | mojhit.pl переходит на kie.ai API вместо локального suno‑api

- **Причина:** Избавление от Python‑зависимостей (не нужен Python 3.12+), использование коммерческого сервиса с SLA, вебхуками, лимитами.
- **Сделанные изменения (кодером):**
  1. В `server/.env` добавлены `KIE_API_KEY`, `KIE_API_BASE_URL`, `KIE_CALLBACK_BASE_URL`.
  2. Создан модуль `server/kie.js` с методами `generate` и `getTaskStatus`, обработкой ошибок 402 (недостаточно кредитов), 429 (лимит), 422 (валидация).
  3. Обновлён `server/index.js`: заменён импорт `suno` на `kie`, обновлён POST `/api/suno/generate` (создание записи в БД, вызов kie.ai, возврат `{ taskId, dbId }`), добавлен POST `/api/webhooks/kie` для колбэков, обновлён GET `/api/suno/status/:id` с polling fallback.
- **Текущий статус:**
  - Клиент (React) работает на `localhost:5173`.
  - Сервер запускается, но может падать из‑за отсутствия таблиц в Supabase.
  - Папка `suno-api` и файл `server/suno.js` удалены — Python‑зависимостей больше нет.
- **Следующие шаги:**
  1. **Создать таблицы в Supabase:** `users` (если нет) и `kie_tasks` (по схеме из инструкции).
  2. **Настроить ngrok** для публичного колбэк‑URL, обновить `KIE_CALLBACK_BASE_URL`.
  3. **Поправить фронтенд:** адаптировать под новый формат ответа (`{ taskId, dbId }`), увеличить лимит промпта до 500 символов, добавить выбор модели.
  4. **Протестировать:** авторизацию, генерацию через kie.ai, получение колбэка.
- **Преимущества новой архитектуры:** Нет Python‑зависимостей, готовый колбэк‑механизм, лучше для продакшена, проще масштабировать.
# 2026-04-15 — Daily Log

## [2026-04-15 15:13] infra | Qwen provider switched to OpenRouter
- Alex asked to switch `qwen` provider from DashScope to OpenRouter for better pricing
- OpenRouter endpoint: `https://openrouter.ai/api/v1`
- OpenRouter API key stored in `openclaw.json` config (models.providers.qwen)
- Model on OpenRouter requires full path: `qwen/qwen3.6-plus`
- First patch attempt failed (validation error: array vs object, unrecognized keys)
- Second patch succeeded — qwen provider now points to OpenRouter
- DeepSeek key also stored in same patch: `sk-or-v1-47b2...`
- **IMPORTANT:** API keys are in openclaw.json — sensitive data, don't leak

## [2026-04-15 15:17] infra | Gateway restarted after config change
- Gateway restarted at 15:17 after `openclaw.json` change
- Startup time: ~22.8s
- Webchat and Telegram plugin (`@architect_gemini_bot`) loaded successfully
- Gateway probe: healthy

## [2026-04-15 15:20] infra | DeepSeek API health check
- Tested DeepSeek API directly via curl (`api.deepseek.com/v1/chat/completions`)
- Result: 200 OK, model `deepseek-chat` responded correctly
- API key is valid, DeepSeek endpoint is working
- Gateway is healthy — no crash detected
- Alex asked to "check what crashes with Qwen" — needs more context on what actually broke

## [2026-04-15 15:54] session | Alex asked to switch to deepseek
- Already running on deepseek/deepseek-reasoner as primary
- No action needed — confirmed status

## [2026-04-15 16:13] infra | Web-search provider changed from Gemini to DeepSeek, added image support for DeepSeek and Qwen
- User reported persistent 503 errors from Gemini web-search provider ("The AI service is temporarily overloaded")
- Requested to move `tools.web.search` from `gemini` to `deepseek` and add text+image input capability for DeepSeek and Qwen models
- Applied config patches:
  1. `tools.web.search.provider` changed from `gemini` to `deepseek`
  2. DeepSeek models (`deepseek-reasoner`, `deepseek-chat`) now have `input: ["text", "image"]` (though API may not actually support images)
  3. Qwen models (`qwen-plus`, `qwen3.6-plus`) now have `input: ["text", "image"]` (OpenRouter may reject image inputs)
- Note: Gemini provider remains configured for regular model usage; only web-search switched away
- Pending verification that deepseek-based web-search works (may not, as deepseek lacks built‑in search grounding)
- Tested `web_search` — error `missing_gemini_api_key` indicates provider switch not fully effective; disabled `tools.web.search.enabled = false` to stop errors
- **Result:** web_search tool now disabled, no more Gemini 503 errors

## [2026-04-15 16:25] infra | Vision model added for image analysis via OpenRouter
- User wants ability to analyze images sent in chat (vision capability)
- Clarified that listed models (wan2.6-t2i, qwen-image-plus, etc.) are **image generation** models, not vision/understanding models
- Added vision-language model `ernie-4.5-vl-424b-a47b` (Baidu ERNIE 4.5 VL 424B A47B) via OpenRouter to provider `qwen`
- Model configured with `api: "openai-completions"`, `input: ["text", "image"]`, contextWindow 123000, maxTokens 16000
- Added alias `ernie-vision` for `qwen/ernie-4.5-vl-424b-a47b` in agents.defaults.models
- **Next:** Test image analysis with `image` tool; may need to adjust provider API adapter if OpenRouter vision requests fail

## [2026-04-15 17:34] project | mojhit.pl moved to workspace raw/MH
- User moved project from original location to `C:\Users\Admin\.openclaw\workspace\raw\MH`
- Verified monorepo structure: client (React/Vite/Tailwind/Clerk), server (Express/Supabase/Clerk/Svix), suno-api (Python bridge)
- Updated `memory/wiki/mojhit-architecture.md` with new location details
- **Next:** Await instructions — run dev, audit code, deploy?

## [2026-04-15 17:39] infra | NVIDIA Nemotron 3 Super (free) discovered on OpenRouter
- User found free model `nvidia/nemotron-3-super-120b-a12b:free` on OpenRouter
- Model details: 120B parameter hybrid MoE (Mamba-Transformer), 262K context, free for input/output tokens
- Created configuration patch `nemotron-patch.json` adding both free and paid versions to `qwen` provider
- Added aliases `nemotron-free` and `nemotron` to agents.defaults.models
- **Configuration highlights:**
  - Free version: `input: ["text"]`, `reasoning: true`, zero cost
  - Paid version: `input: ["text"]`, `reasoning: true`, minimal cost (0.0000001/0.0000005)
  - Both support 262144 context window

## [2026-04-15 17:47] infra | Nemotron 3 Super free model test successful
- Launched subagent to test the free model via OpenRouter
- Subagent completed in 3 seconds, used 17K tokens
- **Result:** Model `qwen/nvidia/nemotron-3-super-120b-a12b:free` responded correctly
- **Confirmation:** Integration works, model is available for use in OpenClaw
- **Next steps:** Can be used as reasoning model for complex tasks, potentially as fallback or primary

## [2026-04-15 18:00] nightly | Memory compilation completed
- Nightly Memory Compiler cron job executed
- Extracted new facts from today's log: Qwen provider switch, OpenRouter integration, web‑search disabled, vision model added, Nemotron 3 Super added, mojhit.pl location update
- Updated `memory/wiki/system-resilience.md` with current provider landscape
- No new wiki files created; existing index remains valid

## [2026-04-15 18:28] project | Attempted local launch of mojhit.pl
- Client (React/Vite) started successfully on `http://localhost:5173` after reinstalling dependencies
- Server (Node.js/Express) starts on port 3000 but exits immediately with code 0 — requires investigation
- Suno‑API Python microservice requires Python 3.12+ (current: 3.10.11) — installation blocked
- **Issues identified:**
  1. Python version mismatch for suno‑api (needs >=3.12)
  2. Server instability (possible missing Supabase tables or invalid Clerk keys)
  3. Suno‑API dependency on local Python service at `http://127.0.0.1:8000`
- **Next:** Decide on path — fix Python environment + server issues, or proceed with mock implementation for UI testing
## [2026-04-15 19:10] project | mojhit.pl переходит на kie.ai API вместо локального suno‑api

- **Причина:** Избавление от Python‑зависимостей (не нужен Python 3.12+), использование коммерческого сервиса с SLA, вебхуками, лимитами.
- **Сделанные изменения (кодером):**
  1. В `server/.env` добавлены `KIE_API_KEY`, `KIE_API_BASE_URL`, `KIE_CALLBACK_BASE_URL`.
  2. Создан модуль `server/kie.js` с методами `generate` и `getTaskStatus`, обработкой ошибок 402 (недостаточно кредитов), 429 (лимит), 422 (валидация).
  3. Обновлён `server/index.js`: заменён импорт `suno` на `kie`, обновлён POST `/api/suno/generate` (создание записи в БД, вызов kie.ai, возврат `{ taskId, dbId }`), добавлен POST `/api/webhooks/kie` для колбэков, обновлён GET `/api/suno/status/:id` с polling fallback.
- **Текущий статус:**
  - Клиент (React) работает на `localhost:5173`.
  - Сервер запускается, но может падать из‑за отсутствия таблиц в Supabase.
  - Папка `suno-api` и файл `server/suno.js` удалены — Python‑зависимостей больше нет.
- **Следующие шаги:**
  1. **Создать таблицы в Supabase:** `users` (если нет) и `kie_tasks` (по схеме из инструкции).
  2. **Настроить ngrok** для публичного колбэк‑URL, обновить `KIE_CALLBACK_BASE_URL`.
  3. **Поправить фронтенд:** адаптировать под новый формат ответа (`{ taskId, dbId }`), увеличить лимит промпта до 500 символов, добавить выбор модели.
  4. **Протестировать:** авторизацию, генерацию через kie.ai, получение колбэка.
- **Преимущества новой архитектуры:** Нет Python‑зависимостей, готовый колбэк‑механизм, лучше для продакшена, проще масштабировать.

## [2026-04-15 19:30] project | mojhit.pl: SQL ограничения и polling механизм
- **SQL ограничения** — `task_id` больше не UNIQUE, убрано `NOT NULL` (для pending задач). Исправлено через Supabase SQL Editor.
- **Polling‑механизм** — сервер сам опрашивает kie.ai каждые 30 секунд, если статус `pending`. Обновляет БД автоматически.
- **Первая успешная генерация** — промпт "happy birthday song for Alex", модель `V4`. Статус `completed` через polling.
- **Данные трека в БД:** `audio_url`, `title`, `duration` (143 секунды). Трек отображается во фронтенде ("Moje utwory").

## [2026-04-15 21:10] project | mojhit.pl: проблемы фронтенда и бекенда
- **Проблема:** Трек не воспроизводится, кнопки "Pobierz MP3", "Udostępnij", "Polub" не работают. Отображается только один вариант трека (должно быть два).
- **Причины:**
  - **Воспроизведение:** Возможно, фронтенд использует неверное поле `audio_url` или блокировка CORS.
  - **Кнопки:** Отсутствуют соответствующие эндпоинты бекенда.
  - **Варианты:** Таблица `kie_track_variants` пуста (возможно, внешний ключ не создан или колбэк не сохраняет варианты).
- **Действия:**
  - Создана таблица `kie_track_variants` (уже существует, внешний ключ `fk_kie_track_variants_task_id` уже есть).
  - Обновлён код `index.js` для сохранения всех вариантов треков в колбэке и polling.
  - **Сервер перезапущен** (процесс `glow-crest` на порту 3000).
- **Следующие шаги:**
  - Добавить поле `expired` в таблицу `tracks` для логического удаления старых треков.
  - Создать эндпоинты для скачивания, лайков, шаринга.
  - Проверить сохранение вариантов через fetch статуса.

## [2026-04-15 21:45] project | mojhit.pl: эндпоинты очистки и логическое удаление
- **Добавлены эндпоинты:**
  - `POST /api/admin/mark-expired` — помечает треки старше 14 дней как `expired: true`
  - `POST /api/admin/cleanup-expired` — удаляет треки с `expired: true`
- **Стратегия очистки:** Не зависеть от OpenClaw cron. Использовать внешний cron‑сервис (например, cron‑job.org) или ручной вызов раз в неделю.
- **Фронтенд обновлён кодером:** Добавлен нативный `<audio controls />`, отображение вариантов, политика хранения 14 дней, увеличен таймаут генерации до 5 минут.
- **Осталось починить:** Кнопки действий, сохранение двух вариантов треков, добавление поля `expired` в БД.
- **Следующие действия пользователя:**
  - Выполнить SQL для добавления поля `expired`.
  - Проверить консоль браузера на ошибки кнопок.
  - Выполнить fetch статуса трека для проверки массива `variants`.
- **Статус:** Сервер работает, фронтенд почти готов, остались мелкие баги.

## [2026-04-15 22:30] project | mojhit.pl: исправление ошибок и добавление кнопок
- **Исправлена ошибка "statusData is not defined" в Generator.tsx**: изменена область видимости переменной `statusData` → объявлена `lastStatusData` перед циклом polling.
- **Добавлены эндпоинты для кнопок действий** в `index.js`:
  - `GET /api/tracks/:id/download` — редирект на `audio_url` для скачивания MP3
  - `POST /api/tracks/:id/like` — увеличивает счетчик `likes` в таблице `tracks`
  - `POST /api/tracks/:id/share` — логирует шаринг, возвращает публичную ссылку
- **Добавлены поля в БД** (SQL подготовлен в `server/add_fields_and_function.sql`):
  - `expired BOOLEAN DEFAULT FALSE` — логическое удаление через 14 дней
  - `likes INTEGER DEFAULT 0` — счетчик лайков
  - Индексы для производительности
- **Проблема с отображением вариантов треков**: варианты хранятся в `kie_track_variants`, но трек в таблице `tracks` не связан с задачей kie.ai.
- **Решение**: добавить поле `kie_task_id UUID` в таблицу `tracks` для связи, обновить `POST /api/tracks` для сохранения связи, модифицировать `GET /api/tracks/:id` для подгрузки вариантов.
- **Статус**: сервер работает (`glow-crest`, порт 3000), фронтенд кнопки отправляют запросы, генерация работает, варианты будут отображаться после добавления связи.
- **Осталось**: выполнить SQL добавления `kie_task_id`, протестировать отображение двух вариантов, завершить полировку фронтенда.