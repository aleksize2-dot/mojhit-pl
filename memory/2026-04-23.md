## [2026-04-23 10:44] запуск | Поднятие серверов и ngrok

- **Бекенд:** Порт 3000 — уже работал (PID 4808, старый процесс), health check ✅
- **Фронтенд:** Порт 5173 был занят, Vite переехал на **5174**
- **Ngrok:** Поднят с новым токеном. Туннель на `fetal-hydroxide-wobbly.ngrok-free.dev` — health check проходит ✅

## [2026-04-23 12:00-14:10] dev | Role i Uprawnienia — полный ремонт

**Бекенд (index.js):**
- `GET /api/admin/users/:userId/roles` — теперь реально загружает роли из `user_admin_roles`
- `POST /api/admin/users/:userId/roles` — теперь реально присваивает роль (с проверкой дубликатов)
- `DELETE /api/admin/users/:userId/roles/:roleId` — теперь реально удаляет роль

**Фронтенд (AdminDashboard.tsx):**
- Вкладка "Role i Uprawnienia" — полный CRUD:
  - Создание новой роли с выбором разрешений (чекбоксы по категориям)
  - Редактирование существующей роли
  - Удаление не-системных ролей (с подтверждением)
  - Кнопки "Zaznacz wszystkie" / "Odznacz wszystkie"
- В таблице пользователей добавлена колонка "Role" — кнопка для назначения/снятия ролей
- Модальное окно управления ролями пользователя (добавление/удаление ролей)

**Статус:** Сервер перезапущен, фронтенд обновлён. Готово к тестированию.

## [2026-04-23 14:19-15:59] fix | AdminDashboard — JSX баланс тегов и модалка ролей пользователя
