## [2026-04-29] dev | Video pipeline fix, provider switch, CJ Remi profile, producer template

### 🎬 Video as Final Product
**Changes to backend (`C:\Users\Admin\.openclaw\workspace\raw\MH\server\index.js`):**
- **Video callback** (`/api/webhooks/kie/video`) — now updates the track's `audio_url` with the video URL when video completes. Track becomes video automatically.
- **Track detail** (`GET /api/tracks/:id`) — now returns `video_url`, `video_thumbnail_url`, `video_status` by looking up `video_tasks` via `kie_task_id` and `kie_track_variants`.
- **MyTracks spinner fix** — stale video tasks (>30 min) no longer show spinning `progress_activity` icon.

### 🔧 Ант's Fixes (merged into server/index.js)
- **Admin bypass for track deletion** — admins can delete any track
- **Guest session merge fix** — fixed UUID vs clerk_id bug on registration
- **Out-of-order webhook guard** — ignores late `first` callbacks after `complete`
- **useGeneratorLogic.ts** — waits for `completed` status, not just `variants.length > 0`
- **`/api/video/check`** — searches both `kie_tasks.id` and `kie_track_variants.id`, returns `processing` status

### ⚡ Provider Switch: KIE → SunoAPI
- **Active provider:** `sunoapi.org` (enabled, priority 2)
- **Fallback:** `kie.ai` (enabled, priority 1)  
- **API key:** Added to `.env` (`SUNO_API_KEY`)
- **API settings:** Updated in Supabase `api_settings` table via admin API
- **Reason:** More reliable callbacks, same API structure

### 📝 Producer Prompts (docs)
- **CJ Remi** — full profile in La Luz format: comparison table, system prompt as code block, theme JSON, UI config, lyrics inline format
- **MELO MC** — reformatted to match La Luz template (pink theme, Claude Sonnet)
- **PRODUCER_TEMPLATE.md** — complete template with all sections, quick checklist, inline lyrics format
- **Key improvements:** Conversational algorithm (active detail gathering), time-of-day greeting ban (`NIGDY nie witaj się porą dnia`)

### 🚨 Incidents
- **Wrong project folder:** Work was done in `E:\mojhit\MH\` (old copy) instead of `C:\Users\Admin\.openclaw\workspace\raw\MH\` (real project). Deleted `E:\MH\` (backup) and `E:\mojhit\MH\` (old copy).
- **taskkill /F /IM node.exe** — accidentally killed ALL node processes including OpenClaw. Server and Vite restarted.
- **Ант's Python script** — broke `try/catch` structure with stray `}`. Fixed manually.

### 📁 Project Structure (final)
- **Project root:** `C:\Users\Admin\.openclaw\workspace\raw\MH\` (mojhit monorepo)
- **Backend:** `raw\MH\server\index.js` (port 3000)
- **Frontend:** `raw\MH\client\` (Vite, port 5173)
- **KIE docs:** `raw\MH\KIE\` (copied by Alex)
- **Producer docs:** `raw\MH\docs\` (CJ_REMI_PRODUCER, MELO_MC_PRODUCER, LA_LUZ_PRODUCER, PRODUCER_TEMPLATE)

### 📋 TODO (after holidays / next session)
1. TrackDetail.tsx reactive video polling when video is processing
2. Webhook signature verification for KIE.ai security
3. Test new CJ Remi prompt in production
4. Test sunoapi.org video generation pipeline
5. Maybe port unused code from old copies (video fixes, TTS)
