# 2026-03-17

## OpenClaw / System
- User noted the local OpenClaw webchat UI does not persist visual history on reload (unlike Telegram). Reassured them that my memory is file-based and fully persistent regardless of the UI window state.

## Bubble.io Project: mojhit.pl
- **UI/Layout Strategy:** Continuing native Bubble development for the Hero section. Utilizing a SaaS F-pattern layout with a marketing left column (`Min width: 320px`, `Gap spacing: 24px`) and a functional right column.
- **Login/Registration Card:** Switched focus to building the unauthenticated state card directly in Bubble's responsive engine (right column).
- **Card Design Specs:** White background (`#FFFFFF`), `12px` border radius, soft 10% opacity box-shadow, `24px` padding, `16px` flex row gap.
- **Card Elements Configured:** 
  - "Rejestracja / Logowanie" bold header.
  - Custom Social login buttons (Google, Facebook) built using Groups (`Row`, min-height `44px`, 1px `#E5E7EB` border) with image icons.
  - A "lub" (or) line divider using groups.
  - Email input field and a standard outline login button.
  - Fine-print Terms & Privacy text at the bottom.
- **Mobile Responsive Fixes:** Adjusted `Min width` on columns to ~300px to enable proper flex wrapping (columns stacking vertically on mobile). Fixed horizontal scroll issues by removing `Fixed width` constraints from inputs and text.
- **Header Implementation:** Built a two-layer sticky header:
  - Top "Trust Bar" (grey) with guarantees and stars.
  - Main Navigation with logo, links (`Fit width to content`, centered via `Space between`), and CTA buttons. Configured the Links block to collapse on mobile (`Collapse when hidden` on `< 768px`).
- **Interactive Audio Preview:** Built a scrolling playlist using a `Repeating Group`.
  - Used `Arbitrary text: split by` trick to generate dummy data rows without a database.
  - Set a `Fixed height: 240px` to enforce vertical scrolling.
  - Implemented `Custom States` (`PlayingIndex` as a number) to track the active track and drive conditional UI (highlighting the row and changing the Play icon to a Pause icon based on `Current cell's index`).
- **Next Tasks (Pending):** 
  - Add visual CSS equalizer animations and gradient backgrounds to the active track in the playlist.
  - Build the Counter Strip (social proof stats).
  - Build the "Jak to działa" (How it works) section.
  - Build the Category Swipe Carousel.