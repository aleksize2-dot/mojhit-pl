
- Architecture Locked: Frontend (React/Vanilla + Tailwind), Backend (Node.js/Express), Auth (Clerk - chosen for modern UI), Database (Supabase - Postgres), Audio Storage (Cloudflare R2 - zero egress fees).

- Discussed security & anti-fraud measures: Cloudflare (DDoS), Node.js Rate Limiting (API budget protection), Stripe Radar (card fraud), Supabase RLS (database row-level security).
- Discussed Admin Panel requirements: API key management (Stripe, BLIK, Kie.ai, Gemini), ad/link management, user statistics/management (balance edits, block/unblock), and an affiliate/referral program with payout tracking.

- Discussed Vibe Coding UI/UX design philosophy: AI-first generation (v0.dev, Bolt, Lovable), component libraries (Shadcn, Tailwind UI), and rough sketching (Excalidraw). Validated user's view that Figma is an outdated middleman for agile Vibe Coding.

## System & Tools
- Clarified OpenClaw gateway operations to user: port 18789 is the `node.exe` process running the webchat interface, not a malicious script.
- Confirmed full cross-channel context persistence (Telegram `@architect_gemini_bot` and localhost webchat sharing the same memory context).
- Analyzed OpenClaw logs: confirmed stable gateway operation, resolved bonjour name conflicts, and verified image resizing behavior.

## Project mojhit.pl (AntiGravity / React)
- **Technical Specification (ТЗ):** Began drafting a detailed, step-by-step feature specification based on 3 provided Google Docs and UX/UI references from competitor "Bitlo".
- **Onboarding:** Hero section with a 3-step guide and an option for unauthenticated "Demo generation" (15-30 seconds) vs direct registration.
- **Auth/Registration:** Unified modal with Social Login (Google, Apple, FB), Email + Password, and "Guest Mode". Implemented Bonus Credits upon signup with strict IP/Device anti-fraud limits to prevent abuse.
- **Main Screen (Generator):** Tabbed interface ("Fast start" with prompt vs "Step-by-step" templates). Status bar during generation. Community feed with "Top 5 / Trends" and "New" tracks.
- **Navigation & Profile:** Mobile-first bottom navigation. Sidebar (Drawer) menu displaying a Dual Balance system (Purchased 🪙 vs Bonus ✨), navigation links, VIP tariffs, and Theme/Language toggles.