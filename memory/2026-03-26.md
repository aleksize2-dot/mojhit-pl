- Explained OpenClaw memory architecture to the user: Short-term memory (transcripts) is isolated per session/channel (Telegram vs Dashboard) to save tokens, while long-term memory (MEMORY.md, workspace files) is global and bridges the context across all sessions.

- **2026-03-26 (mojhit.pl Sprint):**
  - **DevOps:** Fixed the OpenClaw Windows Scheduled Task port conflict (18789 vs 18790). Re-installed the task correctly. Created `gemini helper.bat` script on the Desktop for emergency local boot-ups.
  - **Context Synced:** Read the massive `E:\mojhit\TZ_mojhit.md` document containing the full spec.
  - **Generation Engine Flow:** Mapped how user UI tiles (Occasion/Vibe) connect to hidden Supabase `music_templates` (Suno tags + Gemini instruction prompts).
  - **Marketing & Monetization Funnel:** Designed a "Shadow Account" system (`guest_session_id`). Guests generate tracks instantly without auth, hear a 40-sec demo (fade out), and hit a Paywall. Decoy pricing pushes them to a 49 PLN BLIK payment (Stripe). The Stripe webhook auto-creates their Clerk account, transfers track ownership, grants 10 coins, and emails them the MP3 with a temp password. This bypasses RODO/GDPR friction, secures the Lead Magnet, and maximizes impulse conversions.