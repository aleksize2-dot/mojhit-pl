# 2026-04-28: Naprawa Pipeline Generacji Muzyki

## Co zrobiliśmy dzisiaj (sesja nocna):

### 🔧 Krytyczne bugfixy pipeline'u KIE.ai

1. **Zidentyfikowany i naprawiony główny bug (task_id nie zapisywał się do bazy):**
   - Serwer przy zapisie `task_id` próbował też zapisać kolumnę `provider`, która NIE ISTNIEJE w tabeli `kie_tasks`.
   - Supabase rzucał błąd `PGRST204` (Unknown column), który był ignorowany — `task_id` zostawał `null`.
   - Skutek: KIE.ai generował piosenki, ale webhook nie mógł znaleźć zadania w bazie i nic nie wracało do użytkownika.
   - **Fix:** `server/index.js` linia ~1700 — usunięto `provider: usedProvider` z payloadu `update()`.

2. **Ręczne odtworzenie 4 utraconych wariantów piosenek (Grażyna):**
   - 2 zadania (00d32721, ccfc7905) wisiały w statusie `pending` bez `task_id`.
   - Uruchomiono skrypt naprawczy, wczytano metadane z API KIE.ai używając task_id z panelu użytkownika.
   - Wczytano: audio URL, image URL, tytuły, czas trwania.
   - Wstawiono 4 rekordy do tabeli `tracks` z linkami do obrazków z serwera KIE.

3. **Zwiększono limit tagów stylu:**
   - `server/kie.js`: `substring(0, 199)` → `substring(0, 499)` (teraz 500 znaków na tagi stylu).

4. **Zaktualizowano system_prompt wszystkich producentów (DB):**
   - Limit tagów: 120 → 400 znaków.
   - Dodano wzorowe przykłady stylu z BPM, Weirdness %, Style Influence % (3 przykłady realne usera).
   - Dodano RYGORYSTYCZNE zasady dotyczące znaczników muzycznych:
     - ZAKAZ gwiazdek (`*solo gitarowe*`) — Suno to ŚPIEWA dosłownie!
     - ZAKAZ polskich tagów w `[ ]` — Suno śpiewa po polsku.
     - TYLKO angielskie tagi w kwadratowych nawiasach: `[Guitar Solo]`, `[Beat Drop]`, `[Intro]`.

### ✅ Stan systemu po tej sesji:
- Generacja -> zapis task_id -> webhook -> zapis tracków: DZIAŁA poprawnie.
- Producenci wiedzą jak pisać profesjonalne, długie i szczegółowe tagi stylu.
- Producenci NIE piszą już *gwiazdkowych opisów instrumentów* w lirykach.
- 4 tracki Grażyny odtworzono (z audio + obrazkami) i widoczne są w bibliotece.

## Do zrobienia następnym razem:

1. **Gift Funnel (GIFT_FUNNEL_SPEC.md):** SQL dla `track_templates`, endpoint `/api/templates`, strona `/upominek`.
2. **Function Calling dla Trivia:** AI dodaje monety/noty do konta usera za poprawne odpowiedzi w quizie.
3. **Weryfikacja produkcyjna:** Przeprowadzić 2-3 testowe generacje i sprawdzić czy task_id zapisuje się poprawnie od razu.
